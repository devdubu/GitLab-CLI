# GitLab 연결 설정
GITLAB_URL=http://gitlab
GITLAB_TOKEN=your_personal_access_token_here

# vLLM 설정 (Chat 모드에서 사용)
VLLM_URL=http://host.docker.internal:8000
VLLM_MODEL=gpt-oss-120b

# GitLab 초기 root 비밀번호 (신규 인스턴스 구동 시)
GITLAB_ROOT_PASSWORD=changeme
